// SPDX-License-Identifier: MIT
// Placeholder translation unit so the static library has at least one .o
// while subsystem .cpp files are being added incrementally.
namespace ironclad::detail { int _ironclad_link_anchor() { return 0; } }
